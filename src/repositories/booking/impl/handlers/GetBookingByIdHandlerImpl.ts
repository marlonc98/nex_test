import { Room } from "@prisma/client";

const GetBookingByIdHandlerImpl = async (id: number): Promise<Room | undefined> => {
    throw new Error("Method not implemented.");
}

export default GetBookingByIdHandlerImpl;